# List any vulnerability that are to be accepted
# See https://aquasecurity.github.io/trivy/v0.35/docs/vulnerability/examples/filter/ 
# for more details
# e.g. 
# CVE-2022-3996

# Both vulnerabilities below require upgrading react-router, in both cases blocked by dependencies.
# For details see https://thetradedesk.slack.com/archives/G01SS5EQE91/p1745551035526029

# https://thetradedesk.atlassian.net/browse/UID2-5394
GHSA-f46r-rw29-r322 exp:2026-09-16

# https://thetradedesk.atlassian.net/browse/UID2-5395
GHSA-cpj6-fhp6-mr6j exp:2026-09-16

# UID2-6385
CVE-2025-66293 exp:2026-09-16
# urllib3 vulnerabilities - requires Python 3.9+
CVE-2025-66418 exp:2026-09-16
CVE-2025-66471 exp:2026-09-16

# jackson-core async parser DoS - not exploitable, services only use synchronous ObjectMapper API
# See: UID2-6670
GHSA-72hv-8253-57qq exp:2026-09-01

# CVE-2026-18446 — fast-uri (transitive via ajv) (HIGH).
# Not exploitable here: preview/package-lock resolves fast-uri 3.1.4 via ajv; override at
# preview/package.json:71; no direct usage; static docs build
# See: UID2-7633
CVE-2026-18446 exp:2026-11-06

# CVE-2026-69152 — brace-expansion (npm, transitive via minimatch) (HIGH).
# Not exploitable here: preview/package-lock.json:6291 brace-expansion@5.0.8 via minimatch;
# Docusaurus build-time only; no runtime import
# See: UID2-7619
CVE-2026-69152 exp:2026-11-06

# CVE-2025-71329 — image-size (npm, transitive via @docusaurus/mdx-loader) (HIGH).
# Not exploitable here: preview/package-lock.json:9929 pins image-size@2.0.2, transitive via
# @docusaurus/mdx-loader (line 3433). Same Docusaurus static-build model (deploy.yml builds
# then uploads ./build to GitHub Pages). No JXL/HEIF images present; only trusted repo-
# controlled assets are ever parsed, and only during CI build.
# See: UID2-7653
CVE-2025-71329 exp:2026-11-11

# CVE-2025-71330 — image-size (npm, transitive via @docusaurus/mdx-loader) (HIGH).
# Not exploitable here: preview/package-lock.json:9929 pins image-size@2.0.2, same transitive
# path via @docusaurus/mdx-loader (preview/package-lock.json:3433).
# .github/workflows/deploy.yml builds the static site (npm run build) and deploys ./build to
# GitHub Pages; no runtime Node event loop. No direct image-size usage in preview/src and no
# committed .icns files.
# See: UID2-7654
CVE-2025-71330 exp:2026-11-11

# CVE-2026-67213 — nanoid (transitive via postcss) (HIGH).
# Not exploitable here: preview/package-lock.json: nanoid 3.3.16 present once, transitively via
# postcss. Same Docusaurus build-tooling setup as uid2docs; no source usage of nanoid or the
# vulnerable custom* functions.
# See: UID2-7658
CVE-2026-67213 exp:2026-11-11

# GHSA-5p4m-2wfm-xmqj — js-yaml (3.15.0 and 4.3.0, transitive npm dependency) (HIGH).
# Not exploitable here: Same Docusaurus preview stack as uid2docs: js-yaml via gray-matter
# (build-time frontmatter of trusted repo content) and eslint; no runtime service parsing
# untrusted YAML
# See: UID2-7659
GHSA-5p4m-2wfm-xmqj exp:2026-11-11

# CVE-2026-73088 — browserslist (HIGH).
# Not exploitable here: preview/package.json browserslist is a query object; browserslist is
# transitive build tooling only; no browserslist-stats.json, no --stats flag, no opts.stats
# programmatic call
# See: UID2-7802
CVE-2026-73088 exp:2026-12-02

# CVE-2026-73089 — browserslist (HIGH).
# Not exploitable here: preview/package-lock.json:6315 browserslist@4.28.1; identical
# Docusaurus 3.9.2 static-site setup. build-time only, no attacker-reachable query path.
# See: UID2-7803
CVE-2026-73089 exp:2026-12-02
